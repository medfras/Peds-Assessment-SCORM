# Development-only dependencies — not installed in the production Docker image.
# Install for local development and CI testing:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=9.0.3
pytest-asyncio>=0.24.0
